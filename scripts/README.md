The complete syllabus is in syllabus.json. For the first data load, use the Admin CMS or a trusted Firebase Admin script to import it into the `topics` collection.
Each object contains unitCode, unitTitle, code, title, parentId and order.
